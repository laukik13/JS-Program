// let textNew = " hello "

//Advance declration
let textNew = new String(" hello ");


//String method
textNew.trim(" ");

textNew.substring(0,4);

textNew.toUpperCase();

textNew.toLowerCase();

textNew.splice(0,4);